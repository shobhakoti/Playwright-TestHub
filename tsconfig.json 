# TypeScript compiler configuration.
# Enables type checking, IntelliSense, and cleaner code.
# Required to compile TypeScript into JavaScript before execution.

{
  "compilerOptions": {
    "target": "ESNext",
    "module": "CommonJS",
    "outDir": "dist",
    "rootDir": "tests",
    "strict": true,
    "esModuleInterop": true
  }
}
